import React from 'react';
import type { TableRecord } from '../JobDetail';

interface DetailModelProps {
  record: TableRecord;
}

const DetailModel: React.FC<DetailModelProps> = ({ record }) => {
  // 组件实现...
  return (
    <div>
      {/* 组件内容 */}
    </div>
  );
};

export default DetailModel; 